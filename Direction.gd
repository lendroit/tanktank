class_name Direction

# TODO better naming...
enum ENUM {
	RIGHT,
	LEFT,
	UP,
	DOWN
}

const VECTORS = {
	ENUM.RIGHT: Vector2.RIGHT,
	ENUM.LEFT: Vector2.LEFT,
	ENUM.UP: Vector2.UP,
	ENUM.DOWN: Vector2.DOWN
}
